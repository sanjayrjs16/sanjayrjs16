name: Latest blog post workflow
on:
  schedule:
    # Runs every hour
    - cron: '0 * * * *'
jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: sanjayrjs16/blog-post-workflow@master
        with:
          feed_list: 'https://dev.to/feed/sanjayrjs16, https://medium.com/feed/@sanjayrjs16'